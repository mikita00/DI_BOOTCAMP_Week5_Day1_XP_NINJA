import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Exercise_1_API_HTTP_Request',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
